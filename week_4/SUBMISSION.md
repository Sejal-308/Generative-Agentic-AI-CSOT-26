Completed build1 functions with the lessons for week 4 as a reference, nothing much extra except for truncating outputs and refining looks_like_a_path for better path search. 

For build2 as well completed the three functions mostly in reference to the lessons; wrapped everything in try except blocks so that errors are caught effectively.

For build3 I went with this approach: I wrote the storage() function to store the todos in the disk (memory of computer itself) so that incase of a blackout/ system crash etc. the task list can be retrieved. I also added more functionalities in the allpwed status of a particular task: pending, in_progress, blocked, verified. I refined mark_todo function so that it is more strict about assessing if a task is actually complete. 

The github repo i cloned is the AMRVAC (Adaptive Mesh Refinement Versatile Advection Code). It is a repository physics researchers use for simulating and doing data analysis of fluid dynamics, solar surfaces etc. i chose it because I'm interested in doing data analysis for physics research and it would be nice to have a heavy-duty coding assisstant for that. 

The AGENTS.md simply translated how to use the new tools for this week and the information about target repo into plain English. 